# Demo mode — locked-down settings for reliable demonstrations

camera_source: 0
frame_width: 640
frame_height: 480
target_fps: 30

model_path: null
detection_confidence: 0.5
detect_every_n: 1

persistence_frames: 8    # Higher = more stable transitions
stall_threshold: 200

task_name: "Servo Bracket Assembly"
fullscreen: true
window_name: "AR Assembly Copilot"
show_detections: true

log_dir: "logs"
enable_logging: true
