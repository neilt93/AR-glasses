# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
pyyaml>=6.0

# Object detection (install when you have a trained model)
# ultralytics>=8.0.0

# Failure classifier (Layer 4)
# scikit-learn>=1.3.0

# Object detection (install when you have a trained model)
# ultralytics>=8.0.0

# Optional: for future model training
# torch>=2.0.0
# torchvision>=0.15.0
