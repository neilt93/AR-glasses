# Core dependencies
opencv-python>=4.8.0
numpy>=1.24.0
pyyaml>=6.0

# Object detection (YOLO pretrained on COCO — 80 common object classes)
ultralytics>=8.0.0

# Optional: voice output (offline text-to-speech)
# pyttsx3>=2.90

# Optional: hand pose detection
# mediapipe>=0.10.0

# Optional: OCR for text detection in scene
# pytesseract>=0.3.10

# Optional: failure classifier (Layer 4)
# scikit-learn>=1.3.0

# Optional: for future model training
# torch>=2.0.0
# torchvision>=0.15.0
